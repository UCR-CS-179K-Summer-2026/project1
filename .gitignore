# macOS
.DS_Store

# Build output
build/
*.o
*.a
*.so
*.dylib
*.dSYM/
a.out

# Editors
.vscode/
.idea/
*.exe
*.o
.DS_Store

# CMake — always built out-of-source into /build, but ignore in-source
# artifacts too in case someone runs `cmake .` from the repo root by mistake
build/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json

# manually-compiled binaries from the g++ one-liners in file comments
week*/demo
week*/test_lookup
